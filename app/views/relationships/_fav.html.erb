
<% if Relationship.exists?(corp_id: current_corp.id, user_id: user.id) == true %>
  <%= link_to "お気に入り解除", user_relationships_path(user.id), method: :delete, class: "btn btn-danger" %>
<% else %>
  <%= link_to "お気に入り", user_relationships_path(user.id), method: :post, class: "btn btn-success" %>
<% end %>
